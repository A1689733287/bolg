<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gpg.ssm.blog.dao.TypeCateMapper">
    <resultMap id="BaseResultMap" type="com.gpg.ssm.blog.entity.TypeCate">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="typeName" jdbcType="VARCHAR" property="typeName"/>
    </resultMap>

    <sql id="Base_Column_List">
    id, typeName
  </sql>
    <select id="list" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_type_cate
    </select>


    <!--public T queryById(Integer id);-->
    <select id="queryById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_type_cate
        where id = #{id,jdbcType=BIGINT}
    </select>

    <!--public int deleteById(Integer id);-->
    <delete id="deleteById" parameterType="java.lang.Integer">
    delete from t_type_cate
    where id = #{id,jdbcType=BIGINT}
  </delete>

    <!--public int insert(T entity);-->
    <insert id="insert" parameterType="com.gpg.ssm.blog.entity.TypeCate">
        insert into t_type_cate
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="typename != null">
                typeName,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=BIGINT},
            </if>
            <if test="typename != null">
                #{typename,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>

    <!--public int update(T entity);-->
    <update id="update" parameterType="com.gpg.ssm.blog.entity.TypeCate">
        update t_type_cate
        <set>
            <if test="typename != null">
                typeName = #{typename,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
</mapper>